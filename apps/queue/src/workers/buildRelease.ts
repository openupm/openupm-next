export async function buildRelease(
  _name: string,
  _version: string,
): Promise<void> {
  throw new Error(
    'buildRelease worker is not migrated yet. Migrate from openupm/app/jobs/buildRelease.js.',
  );
}
