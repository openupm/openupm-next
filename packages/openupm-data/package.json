{
  "private": "true",
  "name": "openupm-data",
  "version": "0.0.0",
  "description": "Tools to handle openupm data",
  "type": "module",
  "engines": {
    "node": ">= 18.12 <19"
  },
  "main": "build/index.js",
  "devDependencies": {
    "@fast-check/jest": "^1.7.3",
    "@types/jest": "~29.5",
    "@types/node": "~18",
    "@typescript-eslint/eslint-plugin": "~6.2",
    "@typescript-eslint/parser": "~6.2",
    "cross-env": "^7.0.3",
    "eslint": "~8.46",
    "eslint-config-prettier": "~9.0",
    "eslint-plugin-jest": "~27.2",
    "fast-check": "^3.13.2",
    "jest": "~29.7",
    "openupm-test": "file:../openupm-test",
    "prettier": "~3.0",
    "rimraf": "~5.0",
    "ts-api-utils": "~1.0",
    "ts-jest": "~29.1",
    "ts-node": "^10.9.1",
    "ts-patch": "^3.0.2",
    "typescript": "~5.1"
  },
  "scripts": {
    "start": "node build/src/main.js",
    "clean": "rimraf coverage build tmp",
    "prebuild": "npm run lint",
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "build:release": "npm run clean && tsc -p tsconfig.release.json",
    "lint": "eslint . --ext .ts --ext .mts",
    "test": "cross-env NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "prettier": "prettier --config .prettierrc --write .",
    "test:watch": "cross-env NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "prepare": "ts-patch install"
  },
  "author": "Favo Yang <favoyang@users.noreply.github.com>",
  "license": "BSD-3-Clause",
  "dependencies": {
    "dotenv-flow": "^4.0.0",
    "lodash-es": "^4.17.21",
    "openupm-types": "file:../openupm-types",
    "spdx-license-list": "^6.8.0",
    "tslib": "~2.6",
    "typia": "^5.2.4"
  },
  "volta": {
    "node": "18.18.2"
  }
}